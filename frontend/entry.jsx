import ReactDOM from 'react-dom';
import React from 'react';

window.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  const react = <div>I AM REACT</div>;
  ReactDOM.render(react, root);
});
